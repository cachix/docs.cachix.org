# Request revalidation for documents and Pagefind's stable URLs. The custom
# domain's Cloudflare cache rule must respect these origin headers (see README).
/*
  Cache-Control: public, no-cache, max-age=0, must-revalidate

# Astro's fingerprinted assets can retain the host's normal cache policy.
/_astro/*
  ! Cache-Control
